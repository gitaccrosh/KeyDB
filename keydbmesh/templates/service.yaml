apiVersion: v1
kind: Service
metadata:
  name: {{ include "multiactive.fullname" . }}-headless
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: {{ include "multiactive.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: 6379
      name: redis
